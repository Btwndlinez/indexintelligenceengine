import '../globals.css';

export const metadata = {
  title: 'Index Intelligence Engine',
  description: 'One platform. Many verticals. Industry-specific intelligence products powered by IIE.',
};

export default function IIELayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
