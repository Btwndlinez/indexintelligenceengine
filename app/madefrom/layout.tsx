import '../globals.css';

export const metadata = {
  title: 'Made From — Material Intelligence',
  description: 'Industrial waste, scrap metal, and medical waste intelligence platform.',
};

export default function MadeFromLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
